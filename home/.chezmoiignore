# Never accidentally add anything that looks like a secret
*id_rsa*
*id_ed25519*
*id_ansible*
*id_backup_*
*.pem
*.key
keys.txt
.netrc

# Profile-specific files — set via `.profile` data (workstation/server)
{{- if eq .profile "server" }}
.bash_aliases.d/40-workstation.sh
.config/kitty/kitty.conf
{{- end }}
{{- if eq .profile "workstation" }}
.bash_aliases.d/20-docker.sh
.bash_aliases.d/30-cd-shortcuts-server.sh
{{- end }}
